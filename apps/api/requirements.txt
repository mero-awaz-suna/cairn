# Cairn API — Backend Dependencies
# Lightweight build for Railway free tier (4GB image limit)
# torch/transformers excluded — pipeline runs in mock mode

# ── Web Framework ──
fastapi==0.135.2
uvicorn==0.42.0
pydantic==2.12.5
pydantic-settings==2.13.1
python-multipart==0.0.22

# ── Database ──
supabase==2.28.3

# ── AI (lightweight only) ──
google-generativeai>=0.3.0
numpy>=1.26.0,<2.0

# ── Auth ──
python-jose==3.5.0
passlib==1.7.4

# ── Utilities ──
python-dotenv==1.2.2
httpx>=0.27.0
